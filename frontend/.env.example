VITE_USE_MOCK=false
VITE_API_BASE_URL=/api
# Address reachable FROM THE LAPTOP RUNNING VITE. Use backend laptop IPv4 if remote.
BACKEND_URL=http://127.0.0.1:8080
# Keep aligned with MAX_UPLOAD_MB in backend/.env.
VITE_MAX_UPLOAD_MB=200
